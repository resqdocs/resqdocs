<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!-- Kein Theme.SplashScreen-Parent (= core-splashscreen-Library) mehr: die Library
         wurde nie genutzt (kein installSplashScreen()-Aufruf), liefert aber deprecated
         Edge-to-Edge-APIs (setStatusBarColor/setNavigationBarColor/setDecorFitsSystemWindows)
         als toten Bytecode im AAB mit -> Play-Console-Warnung "randlose Anzeige". Splash
         bleibt unveraendert als Fenster-/Hintergrund-Drawable @drawable/splash erhalten. -->
    <style name="AppTheme.NoActionBarLaunch" parent="AppTheme.NoActionBar">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>