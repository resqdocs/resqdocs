app_identifier(ENV["APP_IDENTIFIER"] || "com.example.resqdocs")
apple_id(ENV["APPLE_ID"]) # nur als Fallback; primär App-Store-Connect-API-Key
itc_team_id(ENV["ITC_TEAM_ID"]) # App Store Connect Team-ID (optional)
team_id(ENV["APPLE_TEAM_ID"]) # your-team-id
